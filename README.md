# @POSTMapping </br>
Created a registration page , where details (first name, middel name, last name, age, email and password) are taken from user which is then stored in a hash map. </br>
Then a login page I created which will validate the email and password and will allow a user to login and access details if they want to or not. </br>
